#include <bits/stdc++.h>

using namespace std;

// Complete the factorial function below.
int fact (int n){
    if (n==0)
        return 1;
    return n* fact(n-1);
}

int main()
{
    int a;
    cin>>a;
    cout<<fact(a);
    return 0;
}

